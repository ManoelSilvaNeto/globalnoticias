// Metadados globais do site.
export const SITE = {
  name: 'GlobalNotícias',
  tagline: 'As notícias que importaram hoje, resumidas — com link pra fonte.',
  description:
    'Resumo automático e neutro das principais notícias do Brasil e do mundo, organizadas por categoria, com crédito e link para as fontes originais.',
} as const;

// Newsletter (grátis). Crie a conta em buttondown.com e ponha o usuário aqui;
// vazio = o formulário não aparece. O embed do Buttondown só usa o nome de
// usuário público — não tem chave/segredo na página.
export const NEWSLETTER = {
  buttondownUser: '',
} as const;
